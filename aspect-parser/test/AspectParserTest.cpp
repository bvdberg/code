#include <string>

#include "yaffut.h"
#include "myassert.h"

#include "AspectParser.h"

using namespace std;

struct AspectParserTest { };

TEST(AspectParserTest, testEmptyFile) {
    /*
    char* input = "";
    XmlReader reader(input, 1);
    ASSERT_NULL(reader.getRootXmlNode());
    */
}


/*
TEST(AspectParserTest, testReadEnv) {
    char* input = "<? bla bla ?>";
    XmlReader reader(input, 1);
    ASSERT_NULL(reader.getRootXmlNode());
}
*/

